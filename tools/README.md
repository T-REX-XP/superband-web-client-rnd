# Tools

| Tool | Path | Purpose |
|------|------|---------|
| Debug console | [`debug-console/`](debug-console/) | Low-level Web Bluetooth probe (raw hex, arbitrary Baji packets) |

The primary end-user client lives in [`../web/`](../web/).
