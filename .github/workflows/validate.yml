name: Validate

on:
  push:
    paths:
      - "web/**"
      - ".github/workflows/validate.yml"
  pull_request:
    paths:
      - "web/**"
      - ".github/workflows/validate.yml"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install and build client
        working-directory: web
        env:
          # Simulate project Pages path during CI
          BASE_PATH: /superband-web-client-rnd/
        run: |
          set -euo pipefail
          bun install --frozen-lockfile
          bun run build

      - name: Check dist output
        run: |
          set -euo pipefail
          test -f web/dist/index.html
          test -f web/dist/.nojekyll
          test -f web/dist/404.html
          grep -q '/superband-web-client-rnd/' web/dist/index.html
          ls -la web/dist/
